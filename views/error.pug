extends layout

block content
  .container
    .row
      .col-md-12
        .section-title
          h3.text-center= message
        .space
          h4= error.status
          if(debug)
            pre #{error.stack}
