extends layout

block content
  .container
    .row
      .col-md-12
        .section-title
          h3.text-center= message
        .space
          h4= error.status
          if(debug)
            pre #{error.stack}
          else
            pre
              | ¡Houston tenemos un problema!
              br
              if(error.status == 404)
                | No se ha encontrado la página que buscabas :/
              else
                | Hubo un error interno :O ¡Intentaremos arreglarlo!
